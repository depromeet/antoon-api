version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu/app/deploy
    overwrite: yes

hooks:
  BeforeInstall:
    - location: scripts/docker/setup.sh
      runas: root
  AfterInstall:
    - location: scripts/docker/run.sh
      runas: root